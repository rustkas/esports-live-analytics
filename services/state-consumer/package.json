{
    "name": "@esports/state-consumer",
    "version": "1.0.0",
    "description": "Event consumer - processes events and maintains live match state",
    "type": "module",
    "main": "./src/index.ts",
    "scripts": {
        "dev": "bun run --watch src/index.ts",
        "start": "bun run src/index.ts",
        "build": "bun build src/index.ts --outdir dist --target node",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@esports/shared": "workspace:*",
        "@clickhouse/client": "^0.2.7",
        "bullmq": "^5.1.1",
        "ioredis": "^5.3.2"
    },
    "devDependencies": {
        "bun-types": "^1.0.25",
        "typescript": "^5.3.3"
    }
}