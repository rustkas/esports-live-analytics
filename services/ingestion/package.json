{
    "name": "@esports/ingestion",
    "version": "1.0.0",
    "description": "Event ingestion service - HTTP intake with validation and Redis Streams",
    "type": "module",
    "main": "./src/index.ts",
    "scripts": {
        "dev": "bun run --watch src/index.ts",
        "start": "bun run src/index.ts",
        "build": "bun build src/index.ts --outdir dist --target node",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@esports/shared": "workspace:*",
        "hono": "^4.0.0",
        "ioredis": "^5.3.2",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "bun-types": "^1.0.25",
        "typescript": "^5.3.3"
    }
}